# requirements.txt
# Bot Analisador de Licitações Públicas
# Instale com: pip install -r requirements.txt

# ── HTTP / Core ───────────────────────────────────────────────────────────
httpx>=0.27.0              # HTTP assíncrono (coleta PNCP + chamadas IA)
fastapi>=0.115.0           # API REST
uvicorn[standard]>=0.30.0  # Servidor ASGI
python-dotenv>=1.0.0       # Variáveis de ambiente do .env

# ── IA — escolha um (ou instale os dois) ─────────────────────────────────
# Groq  → grátis em console.groq.com   (llama-3.3-70b, mistral, etc.)
# Gemini → grátis em aistudio.google.com (gemini-2.0-flash, etc.)
# Não é necessário instalar SDK: ambos são chamados via httpx (REST puro)

# ── Telegram Bot ──────────────────────────────────────────────────────────
python-telegram-bot>=21.0  # Bot Telegram

# ── Extração de PDF (editais) ─────────────────────────────────────────────
pdfplumber>=0.11.0         # Extração de texto de PDFs

# ── Agendamento ───────────────────────────────────────────────────────────
schedule>=1.2.0            # Agendamento simples (alternativa ao cron)
